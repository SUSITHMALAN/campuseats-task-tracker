{
  "name": "campuseats-task-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A small CampusEats task tracker lab project",
  "scripts": {
    "start": "node src/tasks.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}